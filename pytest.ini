[pytest]
addopts = -q --maxfail=1 --cov=app --cov=data_loader.py --cov-report=term-missing
markers =
    slow: marks tests as slow (deselect with "-m not slow")
    requires_real_loader: uses live data_loader.get_dataframe_for_user
norecursedirs = .* build dist temp_mode_test temp_mode_test2
