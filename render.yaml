services:
  - type: web
    name: wholesale-analytics-platform
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    autoDeployTrigger: checksPass
    envVars:
      - key: SECRET_KEY
        generateValue: true
