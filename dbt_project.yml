name: northgate_retail_analytics
version: 1.0.0
config-version: 2

profile: northgate_retail_analytics
model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  northgate_retail_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
      +tags: ["governed", "portfolio"]
