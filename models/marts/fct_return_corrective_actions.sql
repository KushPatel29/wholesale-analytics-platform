select
    id as corrective_action_id,
    cause_type,
    cause_value,
    title,
    owner_user_id,
    due_date,
    status,
    baseline_start,
    baseline_end,
    baseline_return_count,
    baseline_order_count,
    baseline_rate_pct,
    baseline_credit_amount,
    baseline_revenue,
    target_rate_pct,
    outcome_start,
    outcome_end,
    outcome_return_count,
    outcome_order_count,
    outcome_rate_pct,
    outcome_credit_amount,
    outcome_revenue,
    realized_impact_amount,
    confidence_label,
    confidence_n,
    approved_at,
    resolved_at,
    created_at,
    updated_at
from {{ ref('stg_return_corrective_action') }}
